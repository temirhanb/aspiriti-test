export enum StatusTaskEnum {
  "Active" = 1,
  "Completed",
  "Deleted"
}

export enum FormAction {
  edit = "edit",
  completed = "completed",
  delete = "delete",
}

export enum StatusRequest{
  LOADING = 'loading',
    SUCCESS = 'completed',
    ERROR = 'error'
}
